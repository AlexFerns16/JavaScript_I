'use strict';

// global scope execution context
function calcAge(birthYear) {
  const age = 2037 - birthYear;
  console.log(firstName);
  return age;
}

// global variable
const firstName = 'Jonas';

// call to function calcAge
calcAge(1991);
